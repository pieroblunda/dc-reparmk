<%
let LoadedRows = 0

if (user.NextRows > RowsCount) { 
    LoadedRows = RowsCount
} else { 
    LoadedRows = user.NextRows 
}
%>
<div class="col-xs-12 text-center">
    <button id="load-data" type="button" class="btn btn-primary" <% if ( LoadedRows == RowsCount || RowsCount == 0) { %> style="display:none;" <% } %>>
        Carica pi&ugrave; risultati
    </button>
    <h1></h1>
</div>